using UnityEngine;

public class Audio_Data : MonoBehaviour
{
    public enum BGMType
    {
        Title,
        Game,
        Result
    }

    public enum SEType
    {
        Jump,
        Grab,
        Release,
        Damage,
        Collect
    }
}
